#include <stdlib.h>
#include <stdio.h>

/**
 * compress - Try to compress the image file
 *            located in `imagePath` and returns
 *            a pointer to the compressed file.
 *
 * Returns: A char pointer representing the image
 *          path.
 */
char *compress(char *imagePath)
{
	return (imagePath);
}

/**
 * toJPEG - Try to convert the PNG image file
 *          located in `imagePath` in JPEG and returns
 *          a pointer to the new file.
 *
 * Returns: A char pointer representing the image
 *          path.
 */
char *toJPEG(char *imagePath)
{
	return (imagePath);
}


/**
 * toPNG - Try to compress the PNG image file
 *         located in `imagePath` to JPEG and returns
 *         a pointer to the new file.
 *
 * Returns: A char pointer representing the image
 *          path.
 */
char *toPNG(char *imagePath)
{
	return (imagePath);
}
